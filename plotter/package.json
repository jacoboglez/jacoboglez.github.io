{
  "name": "plotter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based generative art framework for a pen plotter (EasyDraw / AxiDraw).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "canvas-sketch-util": "^1.10.0",
    "tweakpane": "^4.0.4"
  },
  "devDependencies": {
    "@tweakpane/core": "^2.0.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
